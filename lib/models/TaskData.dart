class TaskData {
  const TaskData(this.id, this.imageString, this.isDone, this.taskDescription, this.input);

  final int id;
  final String imageString;
  final bool isDone;
  final String taskDescription;
  final String input;
}